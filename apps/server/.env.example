# The server loads environment variables from the repository-root `.env`.
# See the root-level `.env.example` for the full, documented list of variables.
#
#   cp ../../.env.example ../../.env
#
# This file is kept as a pointer so the location of env config is discoverable
# from within the server package.
